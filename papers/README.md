# Papers

For each model in `models/`, the paper picked here is the one that
introduces the architectural idea in its original form (or, for two models,
the honest statement that this repo assembled a combination of two real
papers itself). BibTeX for all references is in
[`references.bib`](references.bib).

| Model | Paper | Year | Link |
|---|---|---|---|
| _(filled in as each model is added)_ | | | |

## Why these nine, and not others

_(filled in as models are added -- explains the lineup choice, what each
model isolates, and what was deliberately left out, e.g. ViT is not
duplicated here since it already lives in `cnn-playground`.)_
